This package allows parsing entities from CUE fields with parsers that depend on the provided URL.
Supported entity parsers need to be provided in advance.

Used for "integration" and "build" parsing.
