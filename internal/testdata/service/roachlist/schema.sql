CREATE TABLE IF NOT EXISTS list (
    Id INT GENERATED ALWAYS AS IDENTITY,
    Item varchar(255) NOT NULL,
    PRIMARY KEY(Id)
) LOCALITY REGIONAL BY ROW;

ALTER TABLE list ALTER COLUMN crdb_region SET VISIBLE;